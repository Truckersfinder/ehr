/**
 * Canonical UI (and optional API copy) for the `appointments.reason` field.
 * Import this from client or server instead of hard-coding the same string.
 */
export const APPOINTMENT_REASON_FOR_VISIT_LABEL = "Reason for Visit";
